apiVersion: v1
kind: ConfigMap
metadata:
  name: geth
  namespace: ethereum
  labels:
    app.kubernetes.io/name: geth
data:
  genesis.json: |
    {{- range .Files.Lines "genesis.json" }}
    {{ . }}{{ end }}