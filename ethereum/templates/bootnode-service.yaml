apiVersion: v1
kind: Service
metadata:
  name: bootnode
  namespace: ethereum
spec:
  selector:
    app.kubernetes.io/name: bootnode-registrar
  clusterIP: None
  ports:
    - name: discovery
      port: 30301
      protocol: UDP
    - name: http
      port: 8080